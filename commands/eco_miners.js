module.exports = {
    name: "miner",
    description: "displays the current user's miner menu.",
    alias: ["emr"],
    help: { //message json data
        content: "Using `miner` alone brings up the miner menu\n @'ing a member after the command can display their miners"
    },
    category: "crypto",
    execute(message, args, bot) {
        //check if crypto-space is established
        //
    }
}